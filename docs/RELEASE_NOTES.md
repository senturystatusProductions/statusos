# StatusOS Release Notes

## Current Stable Frontend Release: v0.2.2

The AI Assistant page and styled chat interface are working and committed to `main`.

## Backend Milestone: v0.2.3

The secure Supabase Edge Function named `statusos-ai` has been deployed. The OpenAI key is stored in Supabase and is not exposed in the frontend.

## Next Release: v0.2.4

Connect the browser-based AI Assistant to the deployed Edge Function and verify real responses.
