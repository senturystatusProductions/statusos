# StatusOS v3.3.2 — Daily Stability & Mobile Artist Navigation

- Shows clear Completed states for Morning and Night in Success OS.
- Adds a mobile artist jump menu and sticky artist search/filter controls.
- Adds persistent habit deletion tombstones so old cloud records cannot reappear.
- Creates a one-time pre-upgrade snapshot of all StatusOS local data.
- Preserves current user data and does not seed or replace habits during upgrade.
- Updates version and service-worker cache to v3.3.2.
