# StatusOS v3.4.7 - PWA Cache Recovery

- Forces Home Screen installations to check for the newest service worker.
- Prevents the installed app from remaining on an old cached release.
- Uses network-first loading for the app shell and JavaScript.
- Automatically activates a waiting update and reloads once.
- Clears caches from all previous StatusOS versions during activation.
- Corrects the displayed runtime version to v3.4.7.
